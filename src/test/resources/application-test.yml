micronaut:
  server:
    port: -1
  application:
    name: micronaut-petclinic-test
  views:
    jte:
      dynamic: false

# Enable sample data for tests
petclinic:
  sample-data:
    enabled: true
  notifications:
    enabled: true
    provider: fake

endpoints:
  info:
    sensitive: false
  health:
    details-visible: authenticated
    jdbc:
      enabled: true

info:
  application:
    name: ${micronaut.application.name}
